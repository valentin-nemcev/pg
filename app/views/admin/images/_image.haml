.item(id="img_#{image.id}")
  = image_tag image.image.url(:thumb) #, :title =>h(image.inspect)
  .title
    = image.id 
  = link_to_function("Вставить", "TextileEditor.insertText({canvas:$('textarea[focused]').get(0)}, '!'+'#{image.id}'+'!');" ) 

!!!
%html
  %head
    %meta{'http-equiv'=>'content-type', :content=>'text/html;charset=UTF-8'}
    %title
      Полит-грамота - управление
    = stylesheet_link_tag 'admin'
    = javascript_include_tag :defaults 
    = javascript_include_tag "jquery.blockUI.js"
    = javascript_tag "$.blockUI.defaults.css = {};"
    = javascript_include_tag "jquery.form.js"
  %body
    %table#page-header
      %td.userbox
        - if logged_in? 
          Администратор: #{current_user.name}
          (#{link_to "Выйти", admin_logout_path, { :title => "Выйти", :class => "to_site" } })
        - else 
          Сбой в матрице. Вы не должны видеть эту страницу.
      %td.links
        = navigation_link params[:controller]=='admin/layout_items', 'Верстка', admin_layout_items_path
        |
        = link_to_unless_current 'Статьи', admin_articles_path
        |
        = navigation_link params[:controller]=='admin/images', 'Изображения', admin_images_path
        |
        = navigation_link params[:controller]=='admin/categories', 'Рубрики', admin_categories_path
        |
        = navigation_link params[:controller]=='admin/quotes', 'Цитаты', admin_quotes_path

    %table#navigation
      %td.title
        %a.title{:href=>"/"} Полит-грамота 
      %td.separator → 
      = breadcrumbs(request.path)
    
    #page-content
    = yield 
